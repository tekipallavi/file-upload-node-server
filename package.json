{
  "name": "file-upload-node",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start ecosystem.config.js --env development",
    "pm2-inspect": "pm2 start ecosystem.config.js --env development  --node-args='--inspect'",
    "start-prod": "pm2 start ecosystem.config.js  --env production",
    "stop-all": "pm2 stop all",
    "restart": "pm2 restart server",
    "logs": "pm2 logs",
    "flush": "pm2 flush server",
    "sfsl": "pm2 stop all && pm2 delete all && pm2 flush && pm2 start ecosystem.config.js --env development && pm2 logs"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@huggingface/inference": "^4.5.3",
    "bcrypt": "^6.0.0",
    "body-parser": "^1.20.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.10.0",
    "multer": "^1.4.5-lts.1",
    "newsapi": "^2.4.1",
    "object-hash": "^3.0.0",
    "pm2": "^6.0.8",
    "redis": "^5.8.2",
    "twitter-api-v2": "^1.24.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
